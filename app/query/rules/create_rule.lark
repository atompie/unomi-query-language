create: "CREATE"i data_type create_name in_scope when then?

%condition.lark%

data_type: /RULE/i
create_name: ESCAPED_STRING
when: "WHEN"i expr
then: "THEN"i expr
in_scope: "IN SCOPE"i ESCAPED_STRING

%import common.WS
%ignore WS

COMMENT_VALUE: "--" /[^\n]/*
%ignore COMMENT_VALUE