create: "CREATE"i data_type create_name when then

%condition.lark%

data_type: /(RULE|SEGEMNT)/i
create_name: ESCAPED_STRING
when: "WHEN"i expr
then: "THEN"i expr

%import common.ESCAPED_STRING
%import common.SIGNED_NUMBER
%import common.DECIMAL
%import common.NUMBER
%import common.WS
%ignore WS

COMMENT_VALUE: "--" /[^\n]/*
%ignore COMMENT_VALUE