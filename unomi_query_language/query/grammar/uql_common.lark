%import common.ESCAPED_STRING
%import common.DECIMAL
%import common.NUMBER
%import common.WS
%import common.NEWLINE

value: DECIMAL
        | NUMBER
        | ESCAPED_STRING
array: "[" [value ("," value)*] "]"

COMMENT_VALUE: ("//"|"#") /[^\n]/*