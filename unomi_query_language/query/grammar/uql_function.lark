%import .uql_common (NUMBER, ESCAPED_STRING, WS)

function: FUNCTION_NAME "(" params? ")"
FUNCTION_NAME: /[a-zA-z_0-9]+/
params: [param ("," param)*]
param: ESCAPED_STRING
    | NUMBER
    | DOTTED_FIELD
DOTTED_FIELD: /[a-zA-Z0-9\._\-:]+/

%ignore WS